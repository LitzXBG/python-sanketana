Git Commands

git init
git add <filename>
git add . (adds all unstaged files)
git commit -m "commit message"
git branch -M main
git remote add origin https://github.com/LitzXBG/python-sanketana.git
git push -u origin main



Shell Commands
pwd - present working directory
ls - list
touch - create an empty file
cd - change directory


if snake_rect.colliderect(apple):
    apple_position_x = random.randint(8,800)
    apple_position_y  = random.randint(8,600)

apple_position_x = random.randint(8,800)
apple_position_y  = random.randint(8,600)
